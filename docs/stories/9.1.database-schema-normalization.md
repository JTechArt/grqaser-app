# Story 9.1: Database Schema Normalization

## Status

Draft

## Story

**As a** developer,
**I want** to normalize the database schema by creating separate tables for authors and categories,
**so that** we eliminate data duplication, ensure data integrity, and enable efficient filtering.

## Acceptance Criteria

1. `authors` table created with unique author names
2. `book_categories` table created with unique category names
3. Books table updated with foreign key references to authors and categories
4. Migration script successfully migrates all existing data
5. No data loss during migration
6. Schema documentation updated

## Tasks / Subtasks

- [ ] Task 1: Create authors table schema (AC: 1)
  - [ ] Add migration or schema definition for `authors` table: id (INTEGER PK AUTOINCREMENT), name (VARCHAR(200) UNIQUE NOT NULL), created_at, updated_at.
  - [ ] Add index `idx_authors_name ON authors(name)`.
  - [ ] Update `books-admin-app` DB model or crawler schema. [Source: architecture/epic-9-schema-changes.md#Step-1]

- [ ] Task 2: Create book_categories table schema (AC: 2)
  - [ ] Add migration or schema definition for `book_categories` table: id (INTEGER PK AUTOINCREMENT), name (VARCHAR(100) UNIQUE NOT NULL), created_at, updated_at.
  - [ ] Add index `idx_categories_name ON book_categories(name)`.
  - [ ] Update `books-admin-app` DB model or crawler schema. [Source: architecture/epic-9-schema-changes.md#Step-1]

- [ ] Task 3: Add foreign keys to books table (AC: 3)
  - [ ] Alter books table: `ALTER TABLE books ADD COLUMN author_id INTEGER REFERENCES authors(id)`.
  - [ ] Alter books table: `ALTER TABLE books ADD COLUMN category_id INTEGER REFERENCES book_categories(id)`.
  - [ ] Create indexes: `idx_books_author_id`, `idx_books_category_id`, `idx_books_duration`.
  - [ ] Create composite index: `idx_books_author_category ON books(author_id, category_id)`.
  - [ ] Keep original `author` and `category` columns temporarily. [Source: architecture/epic-9-schema-changes.md#Step-2, Step-5]

- [ ] Task 4: Create migration script (AC: 4, 5)
  - [ ] Write migration that: (1) creates authors and book_categories tables, (2) populates from DISTINCT books.author and books.category (trimmed, excluding 'Unknown Author'/'Unknown'), (3) updates books.author_id and books.category_id via subqueries.
  - [ ] Add data integrity verification queries (before/after counts) per epic-9-schema-changes.md.
  - [ ] Document rollback plan (drop indexes, drop FK columns, drop new tables).
  - [ ] Run migration against development DB and verify no data loss. [Source: architecture/epic-9-schema-changes.md#SQL-Migration-Script, Data-Integrity-Checks]

- [ ] Task 5: Update schema documentation (AC: 6)
  - [ ] Update `docs/architecture/data-models-and-schema.md` with authors, book_categories tables and books table changes.
  - [ ] Update `docs/architecture/epic-9-schema-changes.md` if any implementation deviations occur. [Source: architecture/data-models-and-schema.md]

- [ ] Task 6: Update books-table.js schema (AC: 3)
  - [ ] Update `books-admin-app/src/crawler/schema/books-table.js` (or equivalent) to include author_id, category_id in column list and CREATE statement for new DBs. Ensure crawler uses updated schema when creating fresh DBs. [Source: architecture/data-models-and-schema.md#Books-table]

- [ ] Task 7: Unit tests
  - [ ] Test migration script against a copy of prod DB (or seeded test DB); verify row counts match before/after.
  - [ ] Test rollback script.
  - [ ] Run `npm test` from books-admin-app. [Source: architecture/testing-and-deployment-strategy.md#Books-admin-app]

## Dev Notes

### Architecture Context

- **Single source of truth:** `books-admin-app/src/crawler/schema/books-table.js` defines the books table. Both crawler and database viewer use this. Schema changes must be reflected there and in data-models-and-schema.md. [Source: architecture/data-models-and-schema.md#Books-table]
- **Writer:** Crawler (and books-admin-app for manual edits). Database-viewer and GrqaserApp are read-only. [Source: architecture/data-models-and-schema.md#Canonical-store]

### Data Models — New Tables (Epic 9)

**authors:**
| Column | Type | Constraints |
|--------|------|--------------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT |
| name | VARCHAR(200) | UNIQUE NOT NULL |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP |
| updated_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP |

**book_categories:**
| Column | Type | Constraints |
|--------|------|--------------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT |
| name | VARCHAR(100) | UNIQUE NOT NULL |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP |
| updated_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP |

**books (additions):**
- author_id INTEGER REFERENCES authors(id)
- category_id INTEGER REFERENCES book_categories(id)

Original `author` and `category` columns kept temporarily for backward compatibility. [Source: architecture/epic-9-schema-changes.md]

### Migration Strategy

1. Create authors and book_categories tables.
2. Populate from `SELECT DISTINCT TRIM(author) FROM books WHERE …` (exclude empty, 'Unknown Author').
3. Populate categories similarly (exclude 'Unknown').
4. UPDATE books SET author_id = (SELECT id FROM authors WHERE name = TRIM(books.author)).
5. Same for category_id.
6. Create indexes.
7. Verify: books_with_author_id count matches books_with_author count.

[Source: docs/prd/epic-9.md#Migration-Strategy]

### File Locations

- `books-admin-app/src/crawler/schema/books-table.js` — books table DDL.
- `books-admin-app/src/crawler/schema/` or `books-admin-app/src/db/` — migration scripts.
- `docs/architecture/data-models-and-schema.md` — schema docs.
- `docs/architecture/epic-9-schema-changes.md` — Epic 9 schema spec.

### Testing

- Unit/integration tests for migration against test DB. Verify data integrity, unique constraints, foreign keys. [Source: architecture/testing-and-deployment-strategy.md]

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-27 | 1.0 | Story created | SM (Bob) |

## Dev Agent Record

### Agent Model Used

_TBD_

### Debug Log References

_TBD_

### Completion Notes List

_TBD_

### File List

_TBD_

## QA Results

_TBD_
