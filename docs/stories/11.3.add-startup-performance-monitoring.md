# Story 11.3: Add Startup Performance Monitoring

## Status

Done

## Story

**As a** developer,
**I want** to measure and monitor app startup performance,
**so that** I can identify bottlenecks and track improvements.

## Acceptance Criteria

1. Performance timing utilities created
2. App initialization phases instrumented
3. Database operations instrumented
4. Startup metrics logged to console (dev) or analytics (prod)
5. Performance dashboard available in dev mode

## Tasks / Subtasks

- [x] Task 1: Add performance timing utilities (AC: 1)
  - [x] Create `GrqaserApp/src/utils/performanceMonitor.ts` (or equivalent).
  - [x] Implement `mark(name)` and `measure(name, startMark, endMark?)` methods.
  - [x] Store marks and measures; log to console in __DEV__.
  - [x] Support `getMeasures()` for dashboard. See epic PerformanceMonitor class. [Source: docs/prd/epic-10.md#Performance-Monitoring]

- [x] Task 2: Instrument app initialization (AC: 2)
  - [x] In App.tsx: call `perfMonitor.mark('app-mount')` on mount.
  - [x] After NavigationContainer onReady: `perfMonitor.mark('navigation-ready')` and `perfMonitor.measure('Time to Navigation Ready', 'app-mount', 'navigation-ready')`.
  - [x] After initializeDatabases fulfills: `perfMonitor.measure('Database Initialization', 'app-mount')`.
  - [x] Log measures in __DEV__. [Source: docs/prd/epic-10.md#Usage-Example]

- [x] Task 3: Instrument database operations (AC: 3)
  - [x] In databaseSlice or databaseManager: mark start/end for initAppMetaDb, fetchManagedDatabases, initCatalogDb.
  - [x] Measure each phase: "App Meta DB Init", "Fetch Managed DBs", "Catalog DB Init".
  - [x] Log per-phase timings. [Source: docs/prd/epic-10.md]

- [x] Task 4: Add startup metrics logging (AC: 4)
  - [x] In __DEV__: console.log all measures (e.g. "[Perf] Database Initialization: 2400ms").
  - [x] In prod: optionally send to analytics (fire-and-forget) if analytics service exists; else skip or minimal log.
  - [x] Ensure no PII or sensitive data in logs. [Source: docs/prd/epic-10.md]

- [x] Task 5: Create performance dashboard (AC: 5)
  - [x] In dev mode: add a simple in-app overlay or screen (e.g. dev menu) that shows `getMeasures()` as a list or table.
  - [x] Optional: trigger via shake gesture or __DEV__ flag.
  - [x] Display: mark name → duration (ms). [Source: docs/prd/epic-10.md]

- [x] Task 6: Unit tests
  - [x] Test performanceMonitor mark/measure logic.
  - [x] Test that measures are recorded correctly.
  - [x] Run `npm test` from GrqaserApp. [Source: architecture/testing-and-deployment-strategy.md]

## Dev Notes

### PerformanceMonitor API

```typescript
class PerformanceMonitor {
  mark(name: string): void;
  measure(name: string, startMark: string, endMark?: string): number;
  getMeasures(): Record<string, number>;
  reset(): void;
}
```

- In __DEV__, log each mark and measure to console. [Source: docs/prd/epic-10.md#Performance-Monitoring]

### Usage in App.tsx

```typescript
useEffect(() => {
  perfMonitor.mark('app-mount');
  dispatch(initializeDatabases()).then(() => {
    perfMonitor.measure('Database Initialization', 'app-mount');
  });
}, [dispatch]);

// NavigationContainer onReady
onReady={() => {
  perfMonitor.mark('navigation-ready');
  perfMonitor.measure('Time to Navigation Ready', 'app-mount', 'navigation-ready');
}}
```

[Source: docs/prd/epic-10.md]

### Metrics to Capture

- Time to Navigation Ready (app-mount → navigation-ready)
- Database Initialization (app-mount → init complete)
- Per-phase: App Meta DB, Fetch Managed DBs, Catalog DB Init. [Source: docs/prd/epic-10.md]

### File Locations

- `GrqaserApp/src/utils/performanceMonitor.ts` (new)
- `GrqaserApp/App.tsx` — instrument mount and navigation
- `GrqaserApp/src/state/slices/databaseSlice.ts` — instrument DB phases (or databaseManager)
- Optional: `GrqaserApp/src/components/PerformanceDashboard.tsx` for dev overlay [Source: architecture/source-tree.md]

### Success Metrics

- Visibility into performance bottlenecks.
- Manual testing: measure TTFR and TTI on device. [Source: docs/prd/epic-10.md#Performance-Benchmarks]

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-27 | 1.0 | Story created | SM (Bob) |
| 2026-02-27 | 1.1 | Added startup performance monitor utility, app/database instrumentation, dev dashboard overlay, and tests | Dev (James) |

## Dev Agent Record

### Agent Model Used

GPT-5 Codex (CLI)

### Debug Log References

No additional debug log file created.

### Completion Notes List

- Added shared `perfMonitor` utility with `mark`, `measure`, `getMeasures`, `reset`, and `subscribe` APIs.
- Instrumented `App.tsx` startup phases: app mount, navigation ready, and database init completion.
- Instrumented database startup phases in `databaseSlice.initializeDatabases`: app meta init, managed DB fetch, catalog DB init.
- Added dev-only `PerformanceDashboard` overlay that renders live measures from `perfMonitor`.
- Added unit tests for monitor behavior and instrumentation paths in `App` and `databaseSlice`.
- Validation completed via lint and targeted `npm test` runs for updated test files.

### File List

- GrqaserApp/src/utils/performanceMonitor.ts
- GrqaserApp/src/components/PerformanceDashboard.tsx
- GrqaserApp/App.tsx
- GrqaserApp/src/state/slices/databaseSlice.ts
- GrqaserApp/__tests__/utils/performanceMonitor.test.ts
- GrqaserApp/__tests__/App.test.tsx
- GrqaserApp/__tests__/state/slices/databaseSlice.test.ts
- docs/stories/11.3.add-startup-performance-monitoring.md

## QA Results

### Review Date: 2026-02-28

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Implementation matches the story. `perfMonitor` provides `mark`, `measure`, `getMeasures`, `reset`, and `subscribe`; logs to console in `__DEV__` only. App.tsx instruments app-mount, navigation-ready (onReady), and database-init-complete (on init fulfill). databaseSlice uses `measurePhase` for App Meta DB Init, Fetch Managed DBs, and Catalog DB Init. PerformanceDashboard shows live measures in dev-only overlay with `pointerEvents="none"`; subscribes for updates. No refactoring performed.

### Refactoring Performed

None. Implementation meets standards.

### Compliance Check

- Coding Standards: ✓ TypeScript, existing patterns
- Project Structure: ✓ Files in correct locations
- Testing Strategy: ✓ Unit tests for perfMonitor; App and databaseSlice instrumentation
- All ACs Met: ✓

### Improvements Checklist

- [x] AC1: Performance timing utilities — performanceMonitor.ts with mark, measure, getMeasures, reset
- [x] AC2: App initialization instrumented — app-mount, navigation-ready, database-init-complete
- [x] AC3: Database operations instrumented — measurePhase for App Meta, Fetch Managed, Catalog DB Init
- [x] AC4: Startup metrics logged — __DEV__ console.log; prod skips (no analytics service)
- [x] AC5: Performance dashboard — PerformanceDashboard overlay in dev mode; getMeasures() display

### Security Review

No PII or sensitive data in logs. Measure names only (e.g. "Database Initialization", "Time to Navigation Ready").

### Performance Considerations

Instrumentation is lightweight (Date.now, Map storage). Dashboard renders only in __DEV__; prod path has no perf impact.

### Files Modified During Review

None.

### Gate Status

Gate: PASS → docs/qa/gates/11.3-add-startup-performance-monitoring.yml

### Recommended Status

✓ Ready for Done
