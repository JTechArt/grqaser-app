# Story 12.1: Database schema and migrations

## Status

Done

## Story

**As a** developer,  
**I want** new database tables (`admin_downloaded_books` and `admin_download_batches`) to track MP3 bulk download batches and per-book download progress,  
**so that** the download pipeline and UI can persist batch history, progress, and status.

## Acceptance Criteria

1. New table `admin_downloaded_books` exists with columns: id, book_id, download_batch_id, local_folder_path, total_size_bytes, part_count, parts_downloaded, status, started_at, completed_at, error_message.
2. New table `admin_download_batches` exists with columns: id, base_folder_path, max_size_bytes, status, books_downloaded, total_size_bytes, started_at, completed_at, duration_seconds, config_json.
3. Migration script creates both tables; migration is reversible (down migration or documented rollback).
4. Tables reference the active database used by books-admin-app.
5. book_id references books.id; download_batch_id references admin_download_batches.id.

## Tasks / Subtasks

- [x] Task 1: Define schema and migration (AC: 1, 2)
  - [x] Add migration file for `admin_downloaded_books` per [Source: docs/feature-requests/mp3-bulk-download-admin.md]
  - [x] Add migration file for `admin_download_batches` per [Source: docs/feature-requests/mp3-bulk-download-admin.md]
  - [x] Document schema in docs/architecture/data-models-and-schema.md (or extension)
- [x] Task 2: Implement migration runner (AC: 3, 4)
  - [x] Create or extend migration runner in books-admin-app to run new migration
  - [x] Ensure migration runs against active DB path (Story 6.2)
  - [x] Add down/rollback capability or document manual rollback
- [x] Task 3: Add indexes and constraints (AC: 5)
  - [x] Add indexes on book_id, download_batch_id for query performance
  - [x] Add foreign key or logical reference to books.id
- [x] Task 4: Verify integration (AC: 4)
  - [x] Run migration against dev DB; verify tables exist
  - [x] Add smoke test or integration test that migration creates tables

## Dev Notes

### Context

Epic 12 adds MP3 bulk download to books-admin-app. This story creates the persistence layer for tracking download batches and per-book progress ([Source: docs/feature-requests/mp3-bulk-download-admin.md]).

### Schema Reference (from feature request)

**admin_downloaded_books:**

| Column | Type | Notes |
|--------|------|-------|
| id | INTEGER | PRIMARY KEY AUTOINCREMENT |
| book_id | INTEGER | NOT NULL; references books.id |
| download_batch_id | TEXT | NOT NULL; e.g. batch-{uuid} |
| local_folder_path | TEXT | NOT NULL |
| total_size_bytes | INTEGER | Sum of MP3 sizes |
| part_count | INTEGER | 1 for single-file; chapter_count for multi-chapter |
| parts_downloaded | INTEGER | Completed parts count |
| status | TEXT | pending, in_progress, completed, failed, paused |
| started_at | TIMESTAMP | |
| completed_at | TIMESTAMP | NULL if failed/paused |
| error_message | TEXT | NULL; set if failed |

**admin_download_batches:**

| Column | Type | Notes |
|--------|------|-------|
| id | TEXT | PRIMARY KEY; e.g. batch-{uuid} |
| base_folder_path | TEXT | NOT NULL |
| max_size_bytes | INTEGER | NOT NULL; e.g. 200GB |
| status | TEXT | preparing, downloading, paused, completed, cancelled |
| books_downloaded | INTEGER | Count completed |
| total_size_bytes | INTEGER | Running sum |
| started_at | TIMESTAMP | |
| completed_at | TIMESTAMP | |
| duration_seconds | INTEGER | Computed or stored |
| config_json | TEXT | JSON: book IDs, pipeline phase, etc. |

### File locations

- **Migrations:** `books-admin-app/src/migrations/` — follow existing pattern (e.g. 001-normalize-authors-categories.js); next migration number.
- **Schema docs:** `docs/architecture/data-models-and-schema.md` — add new section for Epic 12 tables.

### Compatibility

- Uses active DB from Story 6.2; no changes to books table schema. Additive only ([Source: docs/architecture/data-models-and-schema.md]).

### Testing

- Unit or integration test: run migration, verify tables exist and columns match. Per [Source: docs/architecture/testing-and-deployment-strategy.md].

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-15 | 1.0 | Story created from Epic 12 | PM |
| 2026-03-15 | 1.1 | Implemented migration 002, run/rollback scripts, tests, schema doc update | Dev |

## Dev Agent Record

### Agent Model Used

Cursor / Composer

### Debug Log References

- `cd books-admin-app && npm test -- tests/migration-002.test.js` — 9 tests passed
- `node scripts/run-migration-002.js /tmp/test-migration-002.db` — migration ran successfully; tables verified

### Completion Notes List

- **Migration 002** (`002-admin-download-tables.js`): Creates `admin_download_batches` and `admin_downloaded_books` per architecture doc. Includes indexes and FK references to `books(id)` and `admin_download_batches(id)`. Reversible via `down()`.
- **Run script** (`run-migration-002.js`): Uses `getActivePath()` from db-registry when no path provided (Story 6.2). Takes optional `[path-to-db]` arg.
- **Rollback script** (`rollback-migration-002.js`): Executes `migration.down()` for documented rollback.
- **Schema doc**: Added migration 002 reference to `data-models-and-schema.md` under admin download tables section.
- **Tests**: `migration-002.test.js` verifies table creation, column structure, indexes, FK insert, unique constraint, rollback, and idempotency.

### File List

- `books-admin-app/src/migrations/002-admin-download-tables.js` (new)
- `books-admin-app/scripts/run-migration-002.js` (new)
- `books-admin-app/scripts/rollback-migration-002.js` (new)
- `books-admin-app/tests/migration-002.test.js` (new)
- `docs/architecture/data-models-and-schema.md` (modified)

## QA Results

### Review Date: 2026-03-15

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Implementation is well-structured. Migration 002 follows the existing pattern from 001 (separate table SQL constants, indexes, up/down functions). Run and rollback scripts use `getActivePath()` from db-registry per Story 6.2, with sensible fallback to config default. Both scripts create a timestamped backup before modifying the database. Tests are comprehensive and follow the same approach as migration-001 tests (temporary DB, minimal books table for FK, afterEach cleanup).

### Refactoring Performed

None. Code meets standards and no improvements were necessary.

### Compliance Check

- Coding Standards: ✓ Config over hardcoding (db-registry, env); naming camelCase/PascalCase; data contract documented
- Project Structure: ✓ Migrations in `books-admin-app/src/migrations/`, scripts in `scripts/`, tests in `tests/`
- Testing Strategy: ✓ Integration tests for migration; run against in-memory/tmp DB
- All ACs Met: ✓ See trace below

### Improvements Checklist

- [x] AC1 (admin_downloaded_books table): Verified table and columns via tests
- [x] AC2 (admin_download_batches table): Verified table and columns via tests
- [x] AC3 (reversible migration): Rollback test passes; down() drops in correct order
- [x] AC4 (active DB): Run script uses getActivePath() from db-registry
- [x] AC5 (FKs): book_id REFERENCES books(id), download_batch_id REFERENCES admin_download_batches(id); unique (batch,book) constraint

### Security Review

No concerns. Additive schema only; no new API endpoints; backup created before migration.

### Performance Considerations

Indexes on status, started_at, batch_id, book_id support expected queries. Unique constraint (batch_id, book_id) prevents duplicates.

### Files Modified During Review

None.

### Gate Status

Gate: PASS → docs/qa/gates/12.1-database-schema-and-migrations.yml

### Recommended Status

✓ Ready for Done
