# Story 9.2: Advanced Search Backend

## Status

Done

## Story

**As a** developer,
**I want** to implement backend APIs for advanced search with multiple filter types,
**so that** users can search books by author, category, duration, and text.

## Acceptance Criteria

1. GET /api/v1/authors endpoint returns all unique authors
2. GET /api/v1/categories endpoint returns all unique categories
3. Search API supports multi-select author filter (author_ids array)
4. Search API supports multi-select category filter (category_ids array)
5. Search API supports single-select duration filter
6. Search API supports text search (title, description, author name)
7. All filters use AND logic when combined
8. Empty/null filters are ignored (show all)

## Tasks / Subtasks

- [x] Task 1: Create GET /api/v1/authors endpoint (AC: 1)
  - [x] Add route in books-admin-app: `GET /api/v1/authors`.
  - [x] Response: `[{ id, name, book_count }, ...]`. Compute book_count via JOIN or subquery.
  - [x] Add DB method to list authors with book counts.
  - [x] Wire route to DB layer. [Source: docs/prd/epic-9.md#API-Endpoints]

- [x] Task 2: Create GET /api/v1/categories endpoint (AC: 2)
  - [x] Add route in books-admin-app: `GET /api/v1/categories`.
  - [x] Response: `[{ id, name, book_count }, ...]`.
  - [x] Add DB method to list book_categories with book counts.
  - [x] Wire route to DB layer. [Source: docs/prd/epic-9.md#API-Endpoints]

- [x] Task 3: Update search API with filters (AC: 3, 4, 7, 8)
  - [x] Extend `GET /api/v1/books/search` to accept: author_ids (comma-separated or array), category_ids (comma-separated or array), duration_range, text, page, limit.
  - [x] Implement AND logic: only apply non-empty filters; combine with AND.
  - [x] Response: `{ books, total, page, limit }`.
  - [x] Use author_id and category_id for filtering (requires Story 9.1 done). [Source: docs/prd/epic-9.md#API-Endpoints]

- [x] Task 4: Implement duration filter logic (AC: 5)
  - [x] Map duration_range enum to SQL: "<30" → duration < 30, "30-60" → duration >= 30 AND duration < 60, "60-120", "120-300", "300+".
  - [x] Apply only when duration_range is provided. [Source: docs/prd/epic-9.md#Duration-Filter-Mapping]

- [x] Task 5: Implement text search (AC: 6)
  - [x] Search in books.title, books.description, and author name (via JOIN authors).
  - [x] Use LIKE with parameterized pattern (e.g. `%${term}%`). Handle special chars safely.
  - [x] Apply only when text is provided. [Source: docs/prd/epic-9.md]

- [x] Task 6: Update database.js search methods (AC: 3–8)
  - [x] Refactor or add search method in books-admin-app DB layer to accept author_ids[], category_ids[], duration_range, text, pagination.
  - [x] Build dynamic WHERE clause; use indexed columns (author_id, category_id, duration).
  - [x] Ensure indexes exist (Story 9.1). [Source: architecture/epic-9-schema-changes.md#Advanced-Search-Query-Examples]

- [x] Task 7: Add search indexes (AC: 3–8)
  - [x] Verify idx_books_author_id, idx_books_category_id, idx_books_duration, idx_books_author_category exist (Story 9.1).
  - [x] Add any missing indexes for text search if required. [Source: architecture/epic-9-schema-changes.md#Index-Usage]

- [x] Task 8: Unit tests
  - [x] Test GET /api/v1/authors and GET /api/v1/categories (empty, populated).
  - [x] Test search with each filter alone and combined (author_ids, category_ids, duration_range, text).
  - [x] Test empty filters return all books; pagination works.
  - [x] Run `npm test` from books-admin-app. [Source: architecture/testing-and-deployment-strategy.md]

## Dev Notes

### Dependencies

- **Story 9.1 must be complete** — authors and book_categories tables and author_id/category_id on books must exist.

### API Specifications

**GET /api/v1/authors**
```
Response: [{ id: 1, name: "Author Name", book_count: 5 }, ...]
```

**GET /api/v1/categories**
```
Response: [{ id: 1, name: "Category Name", book_count: 12 }, ...]
```

**GET /api/v1/books/search**
```
Query: author_ids, category_ids, duration_range, text, page, limit
Response: { books: [...], total: number, page: number, limit: number }
```

**Duration mapping:**
| UI Label   | Range (min) | SQL Condition                          |
|------------|-------------|----------------------------------------|
| <30 min    | 0-29        | duration < 30                          |
| <1 hour    | 30-59       | duration >= 30 AND duration < 60       |
| 1-2 hours  | 60-119      | duration >= 60 AND duration < 120      |
| 2-5 hours  | 120-299     | duration >= 120 AND duration < 300     |
| 5+ hours   | 300+        | duration >= 300                        |

[Source: docs/prd/epic-9.md#API-Endpoints, #Duration-Filter-Mapping]

### File Locations

- `books-admin-app/src/routes/` — API routes (books, authors, categories).
- `books-admin-app/src/models/` or `books-admin-app/src/db/` — DB access layer.
- API style: REST under `/api/v1/`. [Source: architecture/tech-stack.md]

### Testing

- Jest/Supertest for API tests. Integration tests with test DB. [Source: architecture/testing-and-deployment-strategy.md]

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-27 | 1.0 | Story created | SM (Bob) |
| 2026-02-27 | 1.1 | Implemented advanced search backend APIs, filter logic, indexes, and tests | Dev (Codex) |

## Dev Agent Record

### Agent Model Used

GPT-5 Codex

### Debug Log References

- `npm test -- --runInBand` (books-admin-app): full suite is currently flaky in this environment with failures in `tests/crawler-api.test.js` (`POST /start when already running returns 409`) and an intermittent `socket hang up` in `tests/books.test.js` (`PATCH /api/v1/books/:id` invalid id case) when run with the full suite.
- `npm test -- --runInBand tests/books.test.js tests/api-routes.test.js`: all targeted tests for Story 9.2 passed.

### Completion Notes List

- Added new REST endpoints: `GET /api/v1/authors` and `GET /api/v1/categories`.
- Added DB-layer methods returning normalized author/category lists with `book_count`.
- Refactored search to support `author_ids`, `category_ids`, `duration_range`, `text`, `page`, `limit` with AND-combined dynamic filters.
- Added duration range mapping with normalization for URL-decoded `300+`.
- Added idempotent index verification/creation in DB connect flow for advanced-search columns.
- Expanded integration tests for new endpoints and advanced search filter combinations.
- Executed Story DoD checklist self-validation; all applicable items addressed for this story scope.

### File List

- books-admin-app/src/models/database.js
- books-admin-app/src/routes/books.js
- books-admin-app/src/routes/authors.js
- books-admin-app/src/routes/categories.js
- books-admin-app/src/server.js
- books-admin-app/tests/create-test-db.js
- books-admin-app/tests/books.test.js
- books-admin-app/tests/api-routes.test.js

## QA Results

### Review Date: 2026-02-27

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Implementation is well-structured and meets Story 9.2 scope. Route parsing, dynamic SQL filter composition, and normalized-table joins are coherent and maintainable. Filter handling correctly applies AND logic and ignores empty inputs.

### Refactoring Performed

No refactoring performed during QA review (advisory review only).

### Compliance Check

- Coding Standards: ✓ PASS
- Project Structure: ✓ PASS
- Testing Strategy: ✓ PASS
- All ACs Met: ✓ PASS

### AC Traceability

- AC1 (`GET /api/v1/authors`): Covered by route implementation and tests in `tests/books.test.js` + `tests/api-routes.test.js`.
- AC2 (`GET /api/v1/categories`): Covered by route implementation and tests in `tests/books.test.js` + `tests/api-routes.test.js`.
- AC3 (multi-select `author_ids`): Covered by `parseMultiIdParam` + `searchBooks` dynamic `IN (...)` and `supports filtering by author_ids` test.
- AC4 (multi-select `category_ids`): Covered by `parseMultiIdParam` + `searchBooks` dynamic `IN (...)` and `supports filtering by category_ids` test.
- AC5 (single-select duration): Covered by `buildDurationRangeCondition()` mapping and `supports duration_range filter` test.
- AC6 (text search over title/description/author): Covered by `searchBooks` `LIKE` clause using `COALESCE(authors.name, books.author)` and text-search tests.
- AC7 (AND logic for combined filters): Covered by dynamic `whereConditions.join(' AND ')` and combined-filters test.
- AC8 (ignore empty/null filters): Covered by route/default parsing and `returns 200 and all books when filters are missing` tests.

### Improvements Checklist

- [x] Endpoints for authors/categories verified
- [x] Advanced search filter combinations verified
- [x] Pagination and empty-filter behavior verified
- [x] No blocking defects identified

### Security Review

PASS. Search uses parameterized SQL placeholders for all user-supplied values; no string-interpolated SQL clauses for inputs were found.

### Performance Considerations

PASS. Relevant indexes are ensured at connect time (`author_id`, `category_id`, `duration`, composite author/category; plus text-support indexes on title/description). Query shape matches normalized schema intent.

### Files Modified During Review

- `docs/stories/9.2.advanced-search-backend.md` (QA Results section)
- `docs/qa/gates/9.2-advanced-search-backend.yml` (new gate file)

### Gate Status

Gate: PASS → `docs/qa/gates/9.2-advanced-search-backend.yml`

### Recommended Status

✓ Ready for Done
