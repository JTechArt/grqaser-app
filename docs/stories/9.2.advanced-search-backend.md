# Story 9.2: Advanced Search Backend

## Status

Draft

## Story

**As a** developer,
**I want** to implement backend APIs for advanced search with multiple filter types,
**so that** users can search books by author, category, duration, and text.

## Acceptance Criteria

1. GET /api/v1/authors endpoint returns all unique authors
2. GET /api/v1/categories endpoint returns all unique categories
3. Search API supports multi-select author filter (author_ids array)
4. Search API supports multi-select category filter (category_ids array)
5. Search API supports single-select duration filter
6. Search API supports text search (title, description, author name)
7. All filters use AND logic when combined
8. Empty/null filters are ignored (show all)

## Tasks / Subtasks

- [ ] Task 1: Create GET /api/v1/authors endpoint (AC: 1)
  - [ ] Add route in books-admin-app: `GET /api/v1/authors`.
  - [ ] Response: `[{ id, name, book_count }, ...]`. Compute book_count via JOIN or subquery.
  - [ ] Add DB method to list authors with book counts.
  - [ ] Wire route to DB layer. [Source: docs/prd/epic-9.md#API-Endpoints]

- [ ] Task 2: Create GET /api/v1/categories endpoint (AC: 2)
  - [ ] Add route in books-admin-app: `GET /api/v1/categories`.
  - [ ] Response: `[{ id, name, book_count }, ...]`.
  - [ ] Add DB method to list book_categories with book counts.
  - [ ] Wire route to DB layer. [Source: docs/prd/epic-9.md#API-Endpoints]

- [ ] Task 3: Update search API with filters (AC: 3, 4, 7, 8)
  - [ ] Extend `GET /api/v1/books/search` to accept: author_ids (comma-separated or array), category_ids (comma-separated or array), duration_range, text, page, limit.
  - [ ] Implement AND logic: only apply non-empty filters; combine with AND.
  - [ ] Response: `{ books, total, page, limit }`.
  - [ ] Use author_id and category_id for filtering (requires Story 9.1 done). [Source: docs/prd/epic-9.md#API-Endpoints]

- [ ] Task 4: Implement duration filter logic (AC: 5)
  - [ ] Map duration_range enum to SQL: "<30" → duration < 30, "30-60" → duration >= 30 AND duration < 60, "60-120", "120-300", "300+".
  - [ ] Apply only when duration_range is provided. [Source: docs/prd/epic-9.md#Duration-Filter-Mapping]

- [ ] Task 5: Implement text search (AC: 6)
  - [ ] Search in books.title, books.description, and author name (via JOIN authors).
  - [ ] Use LIKE with parameterized pattern (e.g. `%${term}%`). Handle special chars safely.
  - [ ] Apply only when text is provided. [Source: docs/prd/epic-9.md]

- [ ] Task 6: Update database.js search methods (AC: 3–8)
  - [ ] Refactor or add search method in books-admin-app DB layer to accept author_ids[], category_ids[], duration_range, text, pagination.
  - [ ] Build dynamic WHERE clause; use indexed columns (author_id, category_id, duration).
  - [ ] Ensure indexes exist (Story 9.1). [Source: architecture/epic-9-schema-changes.md#Advanced-Search-Query-Examples]

- [ ] Task 7: Add search indexes (AC: 3–8)
  - [ ] Verify idx_books_author_id, idx_books_category_id, idx_books_duration, idx_books_author_category exist (Story 9.1).
  - [ ] Add any missing indexes for text search if required. [Source: architecture/epic-9-schema-changes.md#Index-Usage]

- [ ] Task 8: Unit tests
  - [ ] Test GET /api/v1/authors and GET /api/v1/categories (empty, populated).
  - [ ] Test search with each filter alone and combined (author_ids, category_ids, duration_range, text).
  - [ ] Test empty filters return all books; pagination works.
  - [ ] Run `npm test` from books-admin-app. [Source: architecture/testing-and-deployment-strategy.md]

## Dev Notes

### Dependencies

- **Story 9.1 must be complete** — authors and book_categories tables and author_id/category_id on books must exist.

### API Specifications

**GET /api/v1/authors**
```
Response: [{ id: 1, name: "Author Name", book_count: 5 }, ...]
```

**GET /api/v1/categories**
```
Response: [{ id: 1, name: "Category Name", book_count: 12 }, ...]
```

**GET /api/v1/books/search**
```
Query: author_ids, category_ids, duration_range, text, page, limit
Response: { books: [...], total: number, page: number, limit: number }
```

**Duration mapping:**
| UI Label   | Range (min) | SQL Condition                          |
|------------|-------------|----------------------------------------|
| <30 min    | 0-29        | duration < 30                          |
| <1 hour    | 30-59       | duration >= 30 AND duration < 60       |
| 1-2 hours  | 60-119      | duration >= 60 AND duration < 120      |
| 2-5 hours  | 120-299     | duration >= 120 AND duration < 300     |
| 5+ hours   | 300+        | duration >= 300                        |

[Source: docs/prd/epic-9.md#API-Endpoints, #Duration-Filter-Mapping]

### File Locations

- `books-admin-app/src/routes/` — API routes (books, authors, categories).
- `books-admin-app/src/models/` or `books-admin-app/src/db/` — DB access layer.
- API style: REST under `/api/v1/`. [Source: architecture/tech-stack.md]

### Testing

- Jest/Supertest for API tests. Integration tests with test DB. [Source: architecture/testing-and-deployment-strategy.md]

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-27 | 1.0 | Story created | SM (Bob) |

## Dev Agent Record

### Agent Model Used

_TBD_

### Debug Log References

_TBD_

### Completion Notes List

_TBD_

### File List

_TBD_

## QA Results

_TBD_
