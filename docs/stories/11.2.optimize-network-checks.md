# Story 11.2: Optimize Network Checks

## Status

Done

## Story

**As a** developer,
**I want** to identify and optimize network-related operations during startup,
**so that** network availability doesn't cause 30-second delays.

## Acceptance Criteria

1. All network operations during startup identified and documented
2. Timeouts added to prevent indefinite blocking
3. Non-critical network checks deferred until after UI renders
4. File system operations optimized (caching, lazy loading)
5. SQLite connections don't block on network checks

## Tasks / Subtasks

- [x] Task 1: Audit network operations at startup (AC: 1)
  - [x] Trace startup flow: App.tsx → initializeDatabases → initAppMetaDb, fetchManagedDatabases, initCatalogDb.
  - [x] Document RNFS operations: exists(), stat(), readDir() — may timeout on slow network/storage.
  - [x] Document SQLite openDatabase — may wait for file system.
  - [x] Document startNetworkMonitor (NetInfo) — defer if it causes delays.
  - [x] Create audit doc or inline comments. [Source: docs/prd/epic-10.md#Potential-Bottlenecks]

- [x] Task 2: Add timeout to RNFS operations (AC: 2)
  - [x] Create `withTimeout<T>(promise, timeoutMs, errorMessage)` utility in `GrqaserApp/src/utils/`.
  - [x] Wrap RNFS.exists(), stat(), readDir() (or equivalent) with timeout (e.g. 5s).
  - [x] On timeout, fail gracefully (e.g. assume file absent, log warning).
  - [x] Use in database initialization path. [Source: docs/prd/epic-10.md#Optimization-2]

- [x] Task 3: Optimize database file checks (AC: 4)
  - [x] Cache RNFS results where safe (e.g. file existence) to avoid repeated blocking calls.
  - [x] Lazy-load file checks where possible (defer until needed).
  - [x] Consider parallel checks if multiple DB paths are validated.
  - [x] Avoid redundant stat/readDir calls. [Source: docs/prd/epic-10.md]

- [x] Task 4: Review SQLite connection timeout (AC: 5)
  - [x] Ensure SQLite.openDatabase() does not block indefinitely.
  - [x] Add timeout or use native timeout option if supported by react-native-sqlite-storage.
  - [x] Ensure DB init doesn't wait on network (SQLite is local; only RNFS may be slow).
  - [x] Document any SQLite timeout behavior. [Source: docs/prd/epic-10.md#Potential-Bottlenecks]

- [x] Task 5: Defer network monitor initialization (AC: 3)
  - [x] If startNetworkMonitor runs at App mount, defer by 2 seconds: `setTimeout(() => startNetworkMonitor(), 2000)`.
  - [x] Return cleanup that clears the timeout and stops the monitor.
  - [x] Ensures UI renders before network monitor init. [Source: docs/prd/epic-10.md#Optimization-3]

- [x] Task 6: Document and apply timeouts in databaseSlice (AC: 2)
  - [x] Wrap initAppMetaDb, fetchManagedDatabases, initCatalogDb (or equivalent) with withTimeout.
  - [x] Suggested: 5s for app meta, 5s for fetch managed DBs, 5s for catalog init; 3s for bundled catalog.
  - [x] On timeout, reject with clear message; databaseSlice sets error state.
  - [x] See epic code example for databaseSlice. [Source: docs/prd/epic-10.md#Optimization-2]

- [x] Task 7: Unit tests
  - [x] Test withTimeout utility.
  - [x] Test initialization with mocked slow RNFS (optional).
  - [x] Run `npm test` from GrqaserApp. [Source: architecture/testing-and-deployment-strategy.md]

## Dev Notes

### Dependencies

- Story 11.1 should be done first (non-blocking init). This story deepens optimization.

### Potential Bottlenecks

1. **RNFS:** exists(), stat(), readDir() — may timeout on slow network/storage.  
2. **SQLite:** openDatabase — may wait for file system.  
3. **Network Monitor:** startNetworkMonitor() — defer.  
4. **Database Manager:** appMetaRepository.listDatabases(), getActiveDatabase() — runs during init. [Source: docs/prd/epic-10.md]

### withTimeout Utility

```typescript
export function withTimeout<T>(
  promise: Promise<T>,
  timeoutMs: number,
  errorMessage = 'Operation timed out'
): Promise<T> {
  return Promise.race([
    promise,
    new Promise<T>((_, reject) =>
      setTimeout(() => reject(new Error(errorMessage)), timeoutMs)
    ),
  ]);
}
```

[Source: docs/prd/epic-10.md]

### Defer Network Monitor

```typescript
useEffect(() => {
  const timerId = setTimeout(() => {
    const stop = startNetworkMonitor();
    return () => { clearTimeout(timerId); stop(); };
  }, 2000);
  return () => clearTimeout(timerId);
}, []);
```

[Source: docs/prd/epic-10.md]

### File Locations

- `GrqaserApp/src/utils/timeout.ts` (new) — withTimeout.
- `GrqaserApp/src/state/slices/databaseSlice.ts` — use withTimeout in initializeDatabases.
- `GrqaserApp/App.tsx` — defer startNetworkMonitor. [Source: architecture/source-tree.md]

### Success Metrics

- Startup time (network available): <3s (Phase 2 target). [Source: docs/prd/epic-10.md]

## Change Log

| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-02-27 | 1.0 | Story created | SM (Bob) |
| 2026-02-27 | 1.1 | Added startup timeout optimization for RNFS/SQLite/database init and deferred network monitor startup | Dev (James) |

## Dev Agent Record

### Agent Model Used

GPT-5 Codex (CLI)

### Debug Log References

No additional debug log file created.

### Completion Notes List

- Added `withTimeout` utility and used it to protect startup-critical operations from indefinite hangs.
- Added startup timeout guards in `databaseSlice.initializeDatabases` for app meta DB init, managed DB lookup, active catalog DB init, and bundled DB init.
- Wrapped SQLite open calls with timeout in `database/connection.ts` to prevent openDatabase hangs.
- Added timeout-safe RNFS wrappers (`exists`, `readDir`, `stat`) and existence caching in `databaseManager` to reduce repeated file-system blocking during startup.
- Deferred `startNetworkMonitor()` by 2 seconds in `App.tsx` so UI renders first, with cleanup for timer and monitor subscription.
- Added tests for `withTimeout`, SQLite open timeout, network monitor deferral, and DB init timeout behavior.
- Validation: `npx eslint` on all touched files and `npm test -- --runInBand` from `GrqaserApp` passed.

### File List

- GrqaserApp/src/utils/timeout.ts
- GrqaserApp/src/state/slices/databaseSlice.ts
- GrqaserApp/src/services/databaseManager.ts
- GrqaserApp/src/database/connection.ts
- GrqaserApp/App.tsx
- GrqaserApp/__tests__/utils/timeout.test.ts
- GrqaserApp/__tests__/database/connection.test.ts
- GrqaserApp/__tests__/App.test.tsx
- GrqaserApp/__tests__/state/slices/databaseSlice.test.ts
- GrqaserApp/__tests__/services/databaseManager.test.ts
- docs/stories/11.2.optimize-network-checks.md

## QA Results

### Review Date: 2026-02-28

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Implementation is thorough and aligns with the story. `withTimeout` utility correctly races a promise against a timeout and clears the timer in a `finally` block. `databaseSlice.initializeDatabases` wraps app meta (5s), managed DB lookup (5s), active catalog (5s), and bundled catalog (3s) with `withTimeout`. `connection.ts` wraps SQLite `openDatabase` and `openBundledDatabase` with 5s timeout. `databaseManager` uses `safeExists` (with 30s TTL cache), `safeReadDir`, and `safeStat`—all timeout-wrapped at 5s. App.tsx defers `startNetworkMonitor` by 2 seconds with proper cleanup. No refactoring performed.

### Refactoring Performed

None. Implementation meets standards.

### Compliance Check

- Coding Standards: ✓ TypeScript, existing patterns
- Project Structure: ✓ Files in correct locations
- Testing Strategy: ✓ Unit tests for withTimeout, connection timeout, App deferral, databaseSlice init timeout
- All ACs Met: ✓

### Improvements Checklist

- [x] AC1: Network operations identified — Inline audit in databaseSlice; RNFS/SQLite/NetInfo documented
- [x] AC2: Timeouts added — withTimeout used in databaseSlice, connection, databaseManager
- [x] AC3: Non-critical network checks deferred — startNetworkMonitor deferred 2s in App.tsx
- [x] AC4: File system ops optimized — safeExists caching, safeReadDir/safeStat with timeouts
- [x] AC5: SQLite connections don't block — openDatabase wrapped with 5s timeout in connection.ts

### Security Review

No security-sensitive changes. N/A.

### Performance Considerations

Timeouts prevent indefinite hangs. Caching reduces repeated RNFS.exists calls. Deferred network monitor allows UI to render first.

### Files Modified During Review

None.

### Gate Status

Gate: PASS → docs/qa/gates/11.2-optimize-network-checks.yml

### Recommended Status

✓ Ready for Done
