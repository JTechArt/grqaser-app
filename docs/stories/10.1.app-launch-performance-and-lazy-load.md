# Story 10.1: App Launch Performance and Lazy Load

## Status

Done

## Story

**As a** mobile user,
**I want** the application to open very quickly without loading all books and images into memory,
**so that** I can start using the app without long waits and the app remains responsive.

## Acceptance Criteria

1. **Fast app open:** The app launches and becomes usable quickly; initial load does not wait for the full catalog or all images to load.
2. **Lazy load catalog:** Book list/catalog data is loaded lazily (e.g., paginated or on-demand), not all books loaded into memory at once.
3. **Collection size from DB:** Where the UI needs the count of books (e.g., collection size), the app uses a count query (e.g., `getCount`) from the database instead of loading all books and then computing the size.
4. **No full preload of images:** Initial launch does not preload all book cover images; images are loaded as needed (lazy load).
5. No regression in browse, search, Library, or playback once data is loaded.

## Tasks / Subtasks

- [x] Task 1: Identify and fix full catalog load at launch (AC: 1, 2)
  - [x] Locate where catalog/books are loaded on app start (e.g., Redux init, home screen, catalog slice)
  - [x] Replace "load all books" with lazy/paginated loading (e.g., load first page or load on screen focus)
  - [x] Ensure app shell becomes interactive before any large data fetch completes

- [x] Task 2: Use getCount (or equivalent) for collection size (AC: 3)
  - [x] Find all places that derive "number of books" or collection size by loading full list (e.g., Library count, catalog count)
  - [x] Add or use a DB count query (e.g., `getCount` in repository/service) and use it for display
  - [x] Remove any pattern that loads all books only to get length/size

- [x] Task 3: Ensure no full image preload at launch (AC: 4)
  - [x] Verify cover images are not preloaded for entire catalog on launch
  - [x] Ensure cover loading is deferred to when lists/screens are shown and images are in view (align with Story 10.6 if needed)

- [x] Task 4: Verify no regressions (AC: 5)
  - [x] Test browse, search, Library, and playback after data is loaded
  - [x] Confirm launch time improvement (e.g., time to interactive) is measurable

## Dev Notes

### Context

Application currently loads too long on open; likely causes include loading all books into memory and loading images up front. Goal: fast time-to-interactive, lazy catalog load, and use DB count for collection size instead of loading all books.

### Relevant Source Tree

```
GrqaserApp/src/
├── state/slices/           ← catalog/library slices may load full list on init
├── database/ or services/  ← add or use getCount for books/collection
├── screens/                ← Home, Library, etc. may trigger full load
└── (see architecture docs for exact paths)
```

### Key Areas to Check

- **App bootstrap / Redux initial state:** Ensure no thunk or effect loads full book list on app start.
- **Catalog/books repository:** Add `getBookCount()` or equivalent that runs `SELECT COUNT(*)` (or similar) instead of `SELECT *` then `.length`.
- **Library/catalog UI:** Use pagination or "load more" / virtual list so only visible or first N items are fetched initially.
- **Cover images:** Handled in Story 10.6; ensure launch path does not preload all covers.

### Testing

- Measure time to interactive before and after (e.g., with 500+ books in DB).
- Verify collection size in UI matches DB count without loading all rows.
- Verify browse, search, Library, playback still work after lazy load.

## Change Log

| Date       | Version | Description        | Author       |
|-----------|---------|--------------------|--------------|
| (current) | 1.0     | Story created      | PM (John)    |
| 2025-02-23 | 1.1   | Implemented lazy catalog load, getStats for counts, no image preload | Dev Agent   |

## Dev Agent Record

### Agent Model Used

(To be filled by Dev Agent)

### Debug Log References

- `npm test -- --testPathPattern="catalogRepository|booksSlice"` — pass
- `npm test -- --testPathIgnorePatterns="SettingsScreen"` — 14 suites, 155 tests pass (SettingsScreen failure pre-existing, mock-related)

### Completion Notes List

- **Task 1:** HomeScreen no longer dispatches `fetchBooks()` (full catalog). It dispatches `fetchBooksPage({ limit: 20, offset: 0 })` and `fetchCatalogStats()` after DB init so the app shell is interactive immediately and only the first page loads. Added `catalogRepository.getBooksPage(limit, offset)` and `getBooksByIds(ids)`; `booksSlice` has `fetchBooksPage`, `fetchBooksByIds`; Library and Favorites load book details by ID via `fetchBooksByIds`.
- **Task 2:** HomeScreen stats (Audiobooks / E-books counts) now use `catalogStats` from `fetchCatalogStats()` (DB `getStats()`), not derived from `books.length` or filtering. `catalogRepository.getBookCount()` and `getStats()` were already present; wired through `booksApi.getStats()` and `fetchCatalogStats` thunk.
- **Task 3:** No full image preload at launch. Grep found no `preload`/`Image.preload`/`FastImage.preload`; covers load only when BookCard renders (FastImage with uri).
- **Task 4:** All relevant tests pass (catalogRepository, booksSlice, database, library, download, App, BookCard, etc.). SettingsScreen.test.tsx still fails (catalogRepository mocked as `{}` causes thunk errors); not introduced by this story.

### File List

- GrqaserApp/src/database/catalogRepository.ts (getBooksPage, getBooksByIds)
- GrqaserApp/src/services/booksApi.ts (getBooksPage, getBooksByIds, getStats, CatalogStats type)
- GrqaserApp/src/state/slices/booksSlice.ts (booksById, catalogStats, fetchBooksPage, fetchBooksByIds, fetchCatalogStats)
- GrqaserApp/src/screens/HomeScreen.tsx (fetchBooksPage, fetchCatalogStats, catalogStats for counts, recentlyPlayed from booksById)
- GrqaserApp/src/screens/LibraryScreen.tsx (booksById, fetchBooksByIds for library entries)
- GrqaserApp/src/screens/FavoritesScreen.tsx (fetchBooksByIds, favoriteBooks from booksById)
- GrqaserApp/__tests__/database/catalogRepository.test.ts (getBooksPage, getBooksByIds)
- GrqaserApp/__tests__/state/slices/booksSlice.test.ts (initial state, fetchBooksPage, fetchBooksByIds, fetchCatalogStats)
- GrqaserApp/__tests__/screens/SettingsScreen.test.tsx (books slice mock extended with full shape)

## QA Results

### Review Date: 2025-02-23

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Implementation is solid and aligns with the story. The app launch path no longer loads the full catalog: `initializeDatabases` does not call `fetchBooks`; HomeScreen dispatches `fetchBooksPage({ limit: 20, offset: 0 })` and `fetchCatalogStats()` after DB init, so the shell becomes interactive quickly. Collection size (Audiobooks / E-books) comes from `catalogStats` via `getStats()` in the repository. Library and Favorites load book details by ID via `fetchBooksByIds`. No full image preload at launch (covers load when BookCard renders). Tests cover `getBooksPage`, `getBooksByIds`, `getStats()`, and slice handlers for `fetchBooksPage`, `fetchBooksByIds`, `fetchCatalogStats`.

### Refactoring Performed

None. No blocking issues; one optional consistency improvement is documented in the gate and below.

### Compliance Check

- Coding Standards: ✓ (TypeScript, Redux Toolkit, catalog through catalogRepository)
- Project Structure: ✓ (repository, services, slices, screens as expected)
- Testing Strategy: ✓ (unit tests for repository and slice; no new e2e required for this story)
- All ACs Met: ✓

### Improvements Checklist

- [x] AC1–5 verified against implementation and tests
- [ ] Consider replacing `fetchBooks()` with `fetchBooksPage` + `fetchCatalogStats` in `databaseSlice` (`loadNewDatabase`, `switchActiveDb`) so switching/loading a DB also uses lazy load (future improvement, not required for this story)

### Security Review

No security impact; catalog is local SQLite read-only.

### Performance Considerations

Launch path is optimized: first page and stats only. The only remaining full-catalog calls are in `loadNewDatabase` and `switchActiveDb` (user-initiated DB change); acceptable for 10.1 scope, with recommendation to align in a follow-up if desired.

### Files Modified During Review

None.

### Gate Status

Gate: PASS → docs/qa/gates/10.1-app-launch-performance-and-lazy-load.yml

### Recommended Status

✓ Ready for Done
