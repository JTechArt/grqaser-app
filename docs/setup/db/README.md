# Setup DB (legacy)

The canonical catalog database has moved to **`data/grqaser.db`** at repo root. See `data/README.md`. This folder is kept for any other setup-related assets; the main DB is in `data/`.
