# Database Viewer — example environment variables
# Copy to .env and set values. Do not commit secrets.

# Server
PORT=3001
HOST=localhost
NODE_ENV=development

# Database (path to crawler SQLite DB or a copy)
DB_PATH=../crawler/data/grqaser.db

# CORS (comma-separated origins or * for allow-all)
CORS_ORIGIN=*

# Rate limiting (optional)
# RATE_LIMIT_WINDOW_MS=900000
# RATE_LIMIT_MAX=100

# Logging
LOG_LEVEL=info
